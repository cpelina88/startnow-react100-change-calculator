A web application built using React that tells you what kind of change is needed after a USD transaction.

Use npm install -> npm run build -> npm start commands in your terminal in order to view project.